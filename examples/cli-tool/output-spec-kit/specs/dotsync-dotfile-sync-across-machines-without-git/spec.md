# Feature Spec — dotsync — dotfile sync across machines without git

> **What & why** — functional requirements only. No tech choices here;
> those live in `plan.md`. Generated by `octalum-bdtb` from a brain-dump
> on 2026-05-16.

## Summary

I'm tired of dotfile repos. Every time I switch laptops I forget to push, or I commit a secret by accident. I want a CLI tool that syncs dotfiles via an encrypted blob stored on S3-compatible storage (R2, B2, whatever).

## Primary domain

`cli` (detected from keyword signals; override if wrong).

## Goals

- _(no explicit goals detected — fill in)_

## User stories

- **US-01** — As a user, I want to *`dotsync add ~/.zshrc` registers a file* so that the product delivers its core value.
- **US-02** — As a user, I want to *`dotsync push` encrypts and uploads diff* so that the product delivers its core value.
- **US-03** — As a user, I want to *`dotsync pull` fetches and merges, prompting on conflict* so that the product delivers its core value.
- **US-04** — As a user, I want to *`dotsync watch` runs in background and pushes on save* so that the product delivers its core value.
- **US-05** — As a user, I want to *Secrets detected via gitleaks-style rules before upload (bail loudly)* so that the product delivers its core value.

## Functional requirements

- `dotsync add ~/.zshrc` registers a file
- `dotsync push` encrypts and uploads diff
- `dotsync pull` fetches and merges, prompting on conflict
- `dotsync watch` runs in background and pushes on save
- Secrets detected via gitleaks-style rules before upload (bail loudly)
- Single binary, no python runtime required on target machines
- Encryption: age (https://age-encryption.org) — battle-tested

## Constraints

- Must be installable via `curl | sh` AND a homebrew tap
- Cannot pull in heavy deps; aiming for <5MB binary

## Out of scope (v0.1)

- _List the things you explicitly will NOT build in the first slice._
- _A spec without an out-of-scope section silently scope-creeps._

## Acceptance criteria

- [ ] Every user story above has at least one end-to-end test.
- [ ] The "out of scope" list has been reviewed with stakeholders.
- [ ] Open questions in `research.md` are resolved or explicitly deferred.

---

_Next: `/speckit.plan` (or read the auto-generated `plan.md` already in this directory)._

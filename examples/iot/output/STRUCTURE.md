# STRUCTURE — Greenhouse monitor for my dad's tomato obsession

> Generated by **brain-dump-to-build** on 2026-05-16.
> Primary domain detected: **iot**.

## Proposed file/folder tree

```
greenhouse-monitor-for-my-dad-s-tomato-obsession/
├── firmware/               # ESP32 / Pi code
│   ├── src/
│   └── platformio.ini
├── ingest/                 # cloud-side service
│   └── main.py
├── dashboard/              # Grafana / Streamlit
├── infra/
│   └── mosquitto.conf
└── README.md

```

## Rationale

- The tree above is a **starting point** tuned for `iot` projects.
- Keep `src/` and `tests/` siblings — most modern tooling expects this.
- `infra/` is included by default so you can codify environment from day one
  (matches OCTALUME Phase 5/6 expectations).
- Refactor freely; this is scaffolding, not scripture.

## Next step

Generate the scaffold:

```bash
brain-dump-to-build "$(cat your_dump.md)" --output-dir ./plan --mode bootstrap
bash ./plan/BUILD_NOW.sh
```

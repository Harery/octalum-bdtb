name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Test (Python ${{ matrix.python-version }} on ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        python-version: ["3.10", "3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"

      - name: Lint
        run: ruff check src tests

      - name: Typecheck
        run: mypy src

      - name: Test
        run: pytest -q --strict-markers --strict-config --cov=brain_dump_to_build --cov-report=term-missing --cov-fail-under=80

      - name: Smoke test CLI
        run: |
          brain-dump-to-build examples/saas/brain-dump.md --output-dir /tmp/out
          test -f /tmp/out/PHASES.md
          test -f /tmp/out/STACK.md
          test -x /tmp/out/BUILD_NOW.sh
